<div id="page">
	<div id="header">
		<h1><%= SiteConfig.application_name %></h1>
	</div>
	
	<%= render :partial => "shared/flash", :locals => { :flash => flash } %>

  <%- width = @view_params[:x2].to_i - @view_params[:x1].to_i -%>

  <div id="transcription_entity_<%= @view_params[:count] %>" class="transcription_entity" style='top:<%= @view_params[:y2] %>px;left:<%= @view_params[:x1] %>px;width:<%= width %>px;'>
  	<%= form_for @annotation do |f| %>
  		<%- @template.entities[1].fields.each do |field| -%>
  		<table>
  			<%- if field.kind == 'textbox' -%>
  			<tr>
  				<td width="80px" class="label"><%= field.name %>:</td><td><%= text_field 'field', field.name %></td>
  			</tr>
  			<%- end -%>
  		<%- end -%>
  			<tr>
  				<td></td><td><%= f.submit "Create" %></td>
  			</tr>
  		</table>

  	<%- end -%>
  	<div class="controls">
  		<%# link_to(image_tag('cancel.png'), "#", :id => "remove_transcription_entity_#{@count}") %>
  	</div>
  </div>
</div>
